#include <stdio.h>
#include <stdlib.h>

int main()
{
 int v=100;
 int w=0;
 int *P;

    printf ("v is found in %p\n", &v);
    printf ("W is found in %p\n", &w);
    printf ("p s found in %p\n", P);

     printf ("V is contains in %d\n", v);
    printf ("W is contains in %d\n", w);
    printf ("*p s contains in %d\n", *P);


}


